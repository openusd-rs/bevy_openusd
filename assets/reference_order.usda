#usda 1.0
(
    upAxis = "Y"
)
def Xform "Root" (
    prepend references = [
        @payload_authoring_content.usda@</Box> (customData = { string label = "box" }),
        @payload_authoring_content.usda@</Ball> (customData = { string label = "sphere" })
    ]
) {}
